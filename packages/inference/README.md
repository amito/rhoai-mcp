# rhoai-mcp-inference

Model inference (KServe/ModelMesh) management plugin for RHOAI MCP Server.

## Installation

```bash
pip install rhoai-mcp-inference
```

## Features

- Create and manage InferenceServices
- Support for single-model (KServe) and multi-model (ModelMesh) serving
- Model deployment status and endpoint management
